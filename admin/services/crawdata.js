const http = require('../../http/http-client')
let crawlDataService = {
    crawl: async function (req) {
        return new Promise(async function (resolve, reject) {
            try {
                let obj =re
                return resolve(true)
            } catch (error) {
                return reject(error)
            }
        })
    }
}



module.exports = crawlDataService